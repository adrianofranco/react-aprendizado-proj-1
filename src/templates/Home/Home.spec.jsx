describe('<Home />', () => {

  it('dummy test one', () => {
    expect(1).toBe(1);
  });

  it('dummy test two', () => {
    expect(1).toBe(1);
  });

  it('dummy test three', () => {
    expect(1).toBe(1);
  });
  

});

